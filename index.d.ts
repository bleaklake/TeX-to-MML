declare module "AM-to-MML" {

    /* interface options {
        width?: number,
        ex?: number,
        em?: number,
        fontURL?: string
    } */

    export default function AMToCHTML(str: string): string;
}